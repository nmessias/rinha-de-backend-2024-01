name: Run Gatling tests

on:
  push:
    branches:
      - main

jobs:
  run-gatling:
    name: Run Gatling
    runs-on: ubuntu-20.04
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
      
      - name: Install Gatling
        run: |
          wget -O gatling.zip https://repo1.maven.org/maven2/io/gatling/highcharts/gatling-charts-highcharts-bundle/3.10.3/gatling-charts-highcharts-bundle-3.10.3-bundle.zip
          unzip gatling.zip -d ~/
          mv ~/gatling-charts-highcharts-bundle-3.10.3 ~/gatling
      
      - name: Clone rinha-de-backend repository
        run: |
          git clone https://github.com/zanfranceschi/rinha-de-backend-2024-q1.git tests
          sed -i 's/220/520/g; s/110/520/g; s/to\(10\)/(200)/g; s/c\(10\)/c(200)/g' tests/load-test/user-files/simulations/rinhabackend/RinhaBackendCrebitosSimulation.scala

      - name: Run app
        run: cd tests/participantes/nmessias && docker compose up --build -d

      - name: Run Gatling
        run: cd tests/ && ./executar-teste-local.sh

      - name: Upload Gatling results to GitHub
        uses: actions/upload-artifact@v4
        with: 
          name: gatling-results
          path: tests/load-test/user-files/results     

  deploy:
    needs: run-gatling

    permissions:
      pages: write
      id-token: write

    environment:
      name: gatling-results  
        
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

